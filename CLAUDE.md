# Writing style

Never use the em dash character (—) anywhere in site content or copy (page text, article bodies, course/category descriptions, UI strings, meta descriptions, etc.). Use a comma, or split into two sentences with a full stop instead. Pick whichever reads more naturally in context.

This applies going forward to all new and edited text on this site, across every Claude Code session working in this repo.
